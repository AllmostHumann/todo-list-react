import styled from "styled-components";

export const Wrapper = styled.main`
    margin: 0 auto;
    text-align: left;
    max-width: 1000px;
    padding: 20px;
    line-height: 1.5;
    background-color: hsl(0, 0%, 85%);
`;