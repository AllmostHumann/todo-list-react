import Container from "../../../common/Container";
import { useParams } from "react-router-dom";


function TaskPage() {
    const { id } = useParams();

    return (
        <Container>

        </Container>
    );
}

export default TaskPage;